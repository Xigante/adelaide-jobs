# ════════════════════════════════════════════════════════════════════
#  Perfil — fonte única da verdade. Tudo no pipeline deriva daqui.
#  Mudou de situação? Mude aqui, não no código.
# ════════════════════════════════════════════════════════════════════
profile_version: "2026.08"

identity:
  name: "Pedro Albertasse"
  arrival_date: 2026-09-24

visa:
  subclass: 500
  condition: 8105
  cap_hours_fortnight: 48
  # Teto operacional abaixo do legal: turno casual estoura, e a
  # responsabilidade pelo limite é sua, não do empregador.
  operational_cap: 44

study:
  provider: "ILSC Adelaide"
  address: "115 Grenfell St, Adelaide SA 5000"
  lat: -34.9235
  lon: 138.6055
  # AM | PM | UNKNOWN — trava ou destrava turnos inteiros.
  # Deixe UNKNOWN até a matrícula confirmar. O scorer trata
  # conflito de horário como penalidade parcial enquanto for UNKNOWN.
  class_pattern: UNKNOWN

english:
  cefr_on_arrival: A2
  # ⇩ ATUALIZE ISTO conforme o inglês melhorar. É o campo que mais muda
  # o resultado. A1 A2 B1 B2 C1 C2.
  #
  # Ao subir de nível, duas coisas acontecem sozinhas: vaga com
  # atendimento ao cliente para de ser penalizada, e o peso da dimensão
  # de inglês cai (porque deixa de ser o gargalo), sendo redistribuído
  # entre as outras. A soma continua 100, então as notas de meses
  # diferentes seguem comparáveis.
  #
  # Depois de mudar aqui, rode:  adelaide-jobs score --rescore
  current_level: A2

transport:
  has_au_drivers_licence: false
  has_car: false
  max_commute_km: 25
  # Turno que termina depois disso tem risco de não ter transporte
  # de volta ao CBD.
  late_return_risk_after: "23:30"

experience_years:
  hospitality: 0
  retail: 0
  warehouse: 0
  cleaning: 0
  office_data: 3

certifications: []   # RSA, White Card, Food Handling: nenhum ainda

objectives:
  primary: CASUAL_ENTRY_BACK_OF_HOUSE
  secondary: OFFICE_ADMIN_DATA
  # Terceiro alvo: a formação dele. Peso pequeno (career_bridge = 4),
  # de propósito — não pode dominar a fila nos primeiros meses. Mas se
  # aparecer vaga boa de dados, games ou design, ela tem que ser vista.
  tertiary: TECH_GAMES_DESIGN

# ─── Pesos do score (somam 100) ─────────────────────────────────────
# Versionados: mude o número da versão ao mexer nos pesos, para
# conseguir comparar resultados antes/depois.
scoring:
  version: "w1"
  weights:
    experience_barrier: 25
    english_load: 20
    visa_hours_fit: 15
    commute: 12
    shift_fit: 10
    certification_barrier: 8
    employer_pattern: 6
    career_bridge: 4
  # Penalidade aplicada a anúncio repostado sem mudança de conteúdo.
  ghost_penalty: 15
  # Score mínimo para a vaga aparecer na fila de candidatura.
  # O relatório HTML mostra TUDO, com filtro por nota — este corte vale
  # para `queue` e para o CSV.
  queue_threshold: 55

# Faixas de nota. Mudar aqui não muda o score, só como ele é rotulado.
#   A+  89-100  candidate-se hoje
#   A   78-88   muito boa
#   A-  70-77   boa
#   B+  62-69   vale olhar
#   B   55-61   talvez
#   C   40-54   só se estiver seco
#   D    0-39   provavelmente não
#   X           bloqueada por um knockout

# Termos que fazem a vaga contar como "ponte de carreira" — trabalho
# de escritório e de dados, que é a formação dele.
#
# TODA palavra desta lista foi contada nos 6.895 anúncios reais de
# Adelaide antes de entrar. O número é quantos títulos a contêm.
# As 35 que não apareciam nenhuma vez saíram, e as 12 repetidas também.
#
# Sobre games: a lista antiga tinha 12 termos de jogos e design
# (unreal, figma, ux designer, liveops, qa tester...). Nenhum aparece
# em Adelaide. A pesquisa em docs/FONTES.md achou UMA vaga de games na
# cidade inteira. Ficaram só dois, para o caso de aparecer.
career_keywords:
  # ── O que Adelaide de fato anuncia como cargo de escritório ────────
  # Isto é a ponte de verdade: emprego de escritório, experiência local.
  - receptionist              # 118 títulos
  - administration officer    # 65
  - customer service officer  # 38
  - administration assistant  # 28
  - accounts payable          # 25
  - payroll officer           # 24
  - finance officer           # 17
  - rostering                 # 15
  - business support          # 14
  - project officer           # 13
  - administrative officer    # 12
  - office administrator      # 12
  - scheduling                # 11
  - accounts receivable       # 10
  - data entry                # 8
  - admin assistant           # 7
  - admin officer             # 5
  - office assistant          # 5
  - records officer           # 5
  - records management        # só na descrição
  - claims officer            # 1
  - data officer              # 2
  - data capture              # 1

  # ── Análise de dados ───────────────────────────────────────────────
  - business analyst          # 20
  - data analyst              # 17
  - reporting                 # 16 no título, 375 nas descrições
  - analytics                 # 12
  - business intelligence     # 7
  - sap                       # 6
  - insights                  # 4
  - workday                   # 4
  - automation                # 4
  - salesforce                # 3
  - python                    # 3
  - data analytics            # 2
  - data governance           # 2
  - power bi                  # 1
  - sql                       # 1
  - excel                     # 0 no título, 17 nas descrições.
                              # Antes casava 633 vezes por causa de
                              # "excellent" — ver extract._compilar
  - spreadsheet               # só na descrição
  - data quality              # só na descrição
  - process improvement       # só na descrição
  - crm                       # só na descrição
  - etl                       # só na descrição
  - tableau                   # só na descrição

  # ── Games e design: Adelaide tem 1 vaga, medida ────────────────────
  - graphic designer          # 4
  - unity                     # 0 no título. Antes casava 70 vezes
                              # dentro de "community"

# Termos que marcam a vaga como back-of-house — trabalho em que o
# inglês baixo funciona, porque não há contato com cliente.
#
# ISTO VALE 20 PONTOS. É a segunda dimensão mais pesada do cálculo:
# um título que casa aqui vira english_load = BOH_MINIMAL, cuja
# mediana de nota é 74. Quem não casa nada cai em UNKNOWN, mediana 64.
# Dez pontos de diferença por uma palavra faltando na lista.
#
# Cada termo foi contado nos 6.895 títulos reais antes de entrar.
# Saíram 13 que não aparecem uma única vez em Adelaide — são
# britânicos: pot wash, potwasher, dish hand, dishy, kitchen porter,
# glass collector, glassie, night fill, night filler, shelf stacker,
# houseperson, accommodation cleaner, linen attendant.
#
# Saiu também "stock", que casava em "Stockroom Manager" e "Stock
# Supervisor". E saíram as formas longas que a fronteira de palavra já
# cobre: "pick packer" e "order picker" casam por "picker" e "packer";
# "warehouse operator" casa por "warehouse"; "kitchen steward" casa
# por "steward".
#
# NÃO estão aqui, de propósito: barista, sales assistant, team member,
# food and beverage attendant, crew member. São de frente de loja, e
# com inglês A2 isso é uma barreira de verdade — marcá-los como
# "inglês mínimo" seria mentir para si mesmo. O extrator já os
# classifica como MEDIUM pela regra de contato com cliente.
boh_keywords:
  # ── Cozinha ────────────────────────────────────────────────────────
  - food services assistant   # 18 títulos
  - kitchen hand              # 15
  - commis                    # 12
  - steward                   # 9  (pega "stewarding" não, ver abaixo)
  - stewarding                # 4
  - catering assistant        # 7
  - kitchenhand               # 5
  - food production           # 4
  - kitchen assistant         # 2
  - kitchen attendant         # 2
  - dishwasher                # 1
  - food service assistant    # 1
  - back of house             # 1

  # ── Supermercado ───────────────────────────────────────────────────
  - nightfill                 # 4  (uma palavra só; "night fill" não existe)
  - trolley                   # 3
  - replenishment             # 2
  - grocery                   # 2

  # ── Armazém e fábrica ──────────────────────────────────────────────
  - storeperson               # 82
  - warehouse                 # 81
  - forklift                  # 42
  - packer                    # 20
  - store person              # 13
  - picker                    # 10
  - process worker            # 9
  - yard hand                 # 8
  - production operator       # 7
  - production worker         # 5
  - machine operator          # 5
  - assembler                 # 5
  - factory hand              # 5
  - loader                    # 4
  - dispatch                  # 3
  - freight handler           # 3

  # ── Limpeza e hotel ────────────────────────────────────────────────
  - cleaner                   # 120
  - housekeeping              # 33
  - cleaning                  # 30
  - housekeeper               # 27
  - room attendant            # 7
  - laundry                   # 6
  - support services          # 6  (Support Services Attendant, SA Health)
  - porter                    # 5
  - valet                     # 5
  - hygiene                   # 4
  - public area               # 1
